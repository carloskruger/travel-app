import getLatLong from '../client/js/app'
import getInformation from '../client/js/app'
describe(getLatLong, () => {
    test("getInformation is defined", () => {
        expect(getInformation).toBeDefined
    });
});
describe(getInformation, () => {
    test("getInformation is defined", () => {
        expect(getInformation).toBeDefined
    });
});